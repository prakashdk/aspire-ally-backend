app: aspir-ally
service: aspir-ally
frameworkVersion: "3"

plugins:
  - serverless-webpack
  - serverless-offline

custom: ${file(./serverless-vars.yml)}

provider:
  name: aws
  runtime: ${self:custom.runtime}
  region: ${self:custom.region}
  timeout: 30
  tags: ${self:custom.tags}
  stage: ${opt:stage,'dev'}
  environment:
    OPENAI_API_KEY: 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
    AWS_ACCESS_KEY_ID: "fakeMyKeyId"
    AWS_SECRET_ACCESS_KEY: "fakeSecretAccessKey"
    AWS_REGION: "us-east-1"
  iam:
    role:
      name: GlobalPerFunctionRole

functions:
  - ${file(./infrastructure/functions/goal.yml)}
  - ${file(./infrastructure/functions/user.yml)}

resources:
  Resources: ${file(./infrastructure/resources/serverless-cfn.yml)}
